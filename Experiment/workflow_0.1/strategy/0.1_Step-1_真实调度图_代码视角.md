# workflow_0.1 Step-1 真实调度图（代码视角）

这张图不是策略设想图，而是“如果你现在在这个仓库里跑 Step-1，背后真实会发生什么”的代码视角流程图。

这次补充一个最重要的理解框架：

`策略层 -> 执行层 -> 产出层`

也就是：

- 先看 `workflow_0.1` 想让 Step-1 做什么
- 再看当前仓库实际上靠哪些脚本去执行
- 最后看这些脚本应该整理成什么 Step-1 产物

阅读原则：

- 绿色：当前仓库里确实存在、可直接调用的脚本或产物
- 橙色：仓库里现存的旧产物，但不是当前脚本的标准输出文件名
- 红色：`workflow_0.1` 视角下还缺的一步，需要补脚本或统一口径

---

## 图 0：Step-1 的三层结构总图

```mermaid
flowchart LR
  classDef strategy fill:#fffdf8,stroke:#17201b,color:#17201b,stroke-width:2px
  classDef exec fill:#eef4fb,stroke:#286299,color:#17201b,stroke-width:2px
  classDef output fill:#eef7f1,stroke:#27704f,color:#17201b,stroke-width:2px
  classDef note fill:#f5f7fa,stroke:#58616a,color:#17201b

  A["策略层<br/>workflow_0.1 定义 Step-1 应该做什么"]:::strategy
  B["执行层<br/>当前仓库真实由哪些脚本在跑"]:::exec
  C["产出层<br/>最终应该整理成哪些 Step-1 数据资产"]:::output

  A --> B --> C

  A1["对应文件<br/>strategy/0.1_Step-1_数据获取流程与思考逻辑.md<br/>README.md"]:::note
  B1["对应入口<br/>bigdata_challenge/data_fetcher/run_all.py<br/>01_price_volume.py<br/>05_sector_momentum.py"]:::note
  C1["对应目标文件<br/>step1_daily_raw_data.csv<br/>step1_stock_summary.csv<br/>step1_sector_summary.csv<br/>step1_data_manifest.csv"]:::note

  A --> A1
  B --> B1
  C --> C1
```

这张总图表达的意思是：

- `strategy/` 里的文档负责定义“Step-1 应该做什么”
- `data_fetcher/` 里的代码负责定义“现在实际上怎么跑”
- `workflow_0.1` 最终要的不是 raw 零散文件，而是标准化的 Step-1 输出表

---

## 图 1：从命令到文件的真实调度图

```mermaid
flowchart TD
  classDef strategy fill:#fffdf8,stroke:#17201b,color:#17201b,stroke-width:2px
  classDef cmd fill:#eef7f1,stroke:#27704f,color:#17201b,stroke-width:2px
  classDef script fill:#eef4fb,stroke:#286299,color:#17201b,stroke-width:2px
  classDef data fill:#f5f7fa,stroke:#58616a,color:#17201b
  classDef old fill:#fff4e5,stroke:#b26b00,color:#17201b,stroke-width:2px
  classDef gap fill:#fff0ec,stroke:#b6432e,color:#17201b,stroke-width:2px

  A0["策略层入口<br/>先看 workflow_0.1 对 Step-1 的定义"]:::strategy
  A["执行层入口<br/>你开始执行 Step-1<br/>从 workflow_0.1 视角看"]:::cmd

  A0 --> A
  A --> B["命令 1<br/>cd bigdata_challenge/data_fetcher<br/>python3 run_all.py --step 1"]:::cmd
  A --> C["命令 2<br/>cd bigdata_challenge/data_fetcher<br/>python3 run_all.py --step 5"]:::cmd

  B --> D["run_all.py<br/>import 01_price_volume.py<br/>调用 fetch_all_stocks()"]:::script
  D --> E["baostock: query_hs300_stocks(date=END_DATE)"]:::script
  E --> F["写出 hs300_stocks.csv<br/>300 只股票，更新日 2026-06-01"]:::data

  D --> G["检查 daily_price_volume.csv 最后日期<br/>决定全量 or 增量更新"]:::script
  G --> H["对 300 只股票循环<br/>query_history_k_data_plus()<br/>字段含 open/high/low/close/volume/amount/turn/pctChg 等"]:::script
  H --> I["过滤停牌日 tradestatus = 1"]:::script
  I --> J["写出或追加 daily_price_volume.csv<br/>245269 行，2023-01-03 ~ 2026-06-01"]:::data

  C --> K["run_all.py<br/>import 05_sector_momentum.py"]:::script
  K --> L["调用 get_sw_industry_classification()"]:::script
  L --> M["主路径：akshare<br/>index_stock_cons_weight_csindex('000300')"]:::script
  M --> N["当前脚本目标输出<br/>hs300_industry_mapping.csv"]:::data
  L --> O["备选路径：baostock<br/>query_stock_industry()"]:::script
  O --> P["备选输出<br/>stock_industry_baostock.csv"]:::data

  K --> Q["调用 get_sector_daily_performance()"]:::script
  Q --> R["akshare: stock_board_industry_name_em()<br/>先拿板块列表"]:::script
  R --> S["只取前 35 个板块<br/>再逐个拉历史日 K"]:::script
  S --> T["当前脚本目标输出<br/>sector_daily_kline.csv"]:::data

  N -.与当前 raw 目录现存文件名不一致.-> U["旧产物：stock_industry.csv<br/>现有 5529 行，更新日 2026-06-01"]:::old
  T -.与当前 raw 目录现存文件名不一致.-> V["旧产物：sector_kline_ths.csv<br/>现有 74070 行，2023-01-03 ~ 2026-06-01"]:::old
  R -.旧列表文件.-> W["旧产物：sector_industry_ths.csv"]:::old

  F --> X["workflow_0.1 想要的 Step-1 资产整理层"]:::gap
  J --> X
  U --> X
  V --> X

  X --> Y["目标 1<br/>step1_daily_raw_data.csv<br/>把日频 raw 行情整理成 workflow_0.1 规定表头"]:::gap
  X --> Z["目标 2<br/>step1_stock_summary.csv<br/>一股一行，汇总近5/20/60日信息 + 行业/板块映射"]:::gap
  X --> AA["目标 3<br/>step1_sector_summary.csv<br/>按六大板块聚合检查"]:::gap
  X --> AB["目标 4<br/>step1_data_manifest.csv<br/>记录 schema、窗口、来源、日期、备注"]:::gap

  AC["可借用参考实现<br/>THU-BDC2026-main/build_step1_stock_raw_sector.py<br/>已经能生成“一股一行”摘要 + 板块划分"]:::script
  AC --> Z
```

---

## 图 2：你以为的 Step-1 和代码里真实 Step-1 的区别

```mermaid
flowchart LR
  classDef ok fill:#eef7f1,stroke:#27704f,color:#17201b,stroke-width:2px
  classDef warn fill:#fff4e5,stroke:#b26b00,color:#17201b,stroke-width:2px
  classDef miss fill:#fff0ec,stroke:#b6432e,color:#17201b,stroke-width:2px

  A["workflow_0.1 策略文档里的 Step-1"]:::ok
  A --> B["拿 daily raw data"]:::ok
  A --> C["拿行业 / 板块映射"]:::ok
  A --> D["归并六大风格板块"]:::ok
  A --> E["输出标准 Step-1 四张表"]:::ok

  F["当前仓库真实已打通"]:::ok
  F --> G["Step 1 抓 HS300 成分股 + 日 K"]:::ok
  F --> H["Step 5 抓行业相关数据 + 板块历史"]:::ok

  I["当前仓库真实存在但有口径风险"]:::warn
  I --> J["现有旧文件名与当前脚本输出文件名不一致"]:::warn
  I --> K["行业映射文件是否正好等于 workflow_0.1 想要的口径，需要再核"]:::warn
  I --> L["板块历史脚本目前只抓前 35 个板块，不是完整全量板块"]:::warn

  M["当前仓库还没在 workflow_0.1 里正式接上"]:::miss
  M --> N["六大风格板块统一映射脚本"]:::miss
  M --> O["Step-1 标准四张表导出脚本"]:::miss
  M --> P["manifest 与 schema_version 固化"]:::miss
```

---

## 真实口径小结

### 1. 你现在真正在跑的“抓数层”

最接近真实入口的是：

```bash
cd bigdata_challenge/data_fetcher
python3 run_all.py --step 1
python3 run_all.py --step 5
```

这两步解决的是：

- 抓沪深 300 成分股列表
- 抓 300 只股票的日频行情
- 抓行业相关映射
- 抓行业板块历史日 K

### 2. `workflow_0.1` 想要的“Step-1 资产层”

这层在策略上已经定义清楚了，但还没有在 `Experiment/workflow_0.1/` 目录下形成一套正式脚本，把 raw 文件稳定转成：

- `step1_daily_raw_data.csv`
- `step1_stock_summary.csv`
- `step1_sector_summary.csv`
- `step1_data_manifest.csv`

### 3. 哪些文件是“现在目录里有，但别轻易当成当前脚本标准输出”的

- `bigdata_challenge/data/raw/stock_industry.csv`
- `bigdata_challenge/data/raw/sector_kline_ths.csv`
- `bigdata_challenge/data/raw/sector_industry_ths.csv`

它们是现有可参考的数据资产，但从当前 `05_sector_momentum.py` 的代码看，标准输出文件名已经写成别的名字了。

### 4. 如果只用一句话解释你跑 Step-1 时背后在干什么

你不是在“直接跑出 workflow_0.1 的最终 Step-1 成果”，而是在先跑一层通用抓数脚本，把原始行情和行业相关数据抓下来；然后还需要再补一层整理脚本，才能变成 `workflow_0.1` 真正定义的 Step-1 资产表。

### 5. 以后看这张图时，建议按这三个问题去读

- 策略层：`workflow_0.1` 希望 Step-1 解决什么问题？
- 执行层：当前仓库到底调用了哪些脚本和数据源？
- 产出层：这些 raw 文件最后有没有被整理成标准 Step-1 结果？
