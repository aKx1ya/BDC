# THU-BDC2026 策略流程与实验方案：结构化流程图

来源背景：`大数据竞赛/THU-BDC2026_策略流程与实验方案.md`

设计目标：把比赛策略从“数据资产 → 特征样本 → 时间切分 → 模型召回Top30 → 精排Top5 → 冻结提交 → 评分复盘”的完整链路画成可执行、可汇报、可检查的流程图。

核心边界：

- 第5步只输出 `candidate_top30.csv`，不直接生成正式提交。
- 第6步从 Top30 精排出最终 Top5 和权重，并生成唯一 `result.csv`。
- 第7步只能在 `result.csv` 冻结后读取本地 `test.csv` 或等待组委会未来行情评分。
- 防未来信息泄漏贯穿所有阶段，任何数据都要能回答：预测日T当天是否已经知道？

---

## 图 1：七步主流程总图

```mermaid
flowchart TD
  classDef data fill:#eef7f1,stroke:#27704f,color:#17201b,stroke-width:1px
  classDef feature fill:#eef4fb,stroke:#286299,color:#17201b,stroke-width:1px
  classDef sample fill:#fff2e8,stroke:#b9612b,color:#17201b,stroke-width:1px
  classDef split fill:#f2eff8,stroke:#71539b,color:#17201b,stroke-width:1px
  classDef model fill:#fff7da,stroke:#a47b12,color:#17201b,stroke-width:1px
  classDef rerank fill:#fff0ec,stroke:#b6432e,color:#17201b,stroke-width:1px
  classDef eval fill:#f1f3f5,stroke:#58616a,color:#17201b,stroke-width:1px
  classDef gate fill:#ffffff,stroke:#111827,color:#111827,stroke-width:2px

  Start["比赛目标<br/>沪深300股票池<br/>最多5只股票<br/>weight总和不超过1"]:::gate

  subgraph D["1. 数据获取 Data Layer"]
    D1["个股日线行情<br/>open close high low volume amount turnover pct_chg"]:::data
    D2["历史沪深300成分股<br/>按effective_start_date和end_date对齐"]:::data
    D3["交易日历与指数行情<br/>确认T+1到T+5交易日"]:::data
    D4["行业 基本面 事件日历<br/>公告日和公开日必须不晚于预测日T"]:::data
    DQ["数据质量检查<br/>缺失 重复 异常 停牌 股票池截面"]:::gate
  end

  subgraph F["2. 特征工程 Feature Layer"]
    F1["价量特征<br/>收益率 均线 波动率 成交量 CLV"]:::feature
    F2["横截面特征<br/>收益排名 成交额排名 行业内排名"]:::feature
    F3["市场与行业特征<br/>沪深300趋势 市场波动 行业动能"]:::feature
    F4["风险 基本面 事件特征<br/>回撤 流动性 PE行业百分位 已公开事件"]:::feature
    FO["特征产物<br/>feature_table_daily<br/>market sector risk fundamental event tables<br/>feature_metadata"]:::feature
  end

  subgraph S["3. 标签 窗口 样本 Sample Layer"]
    S1["预测日T<br/>每个交易日构造一个横截面排序题"]:::sample
    S2["输入窗口X<br/>每只股票过去60日特征<br/>[T-59, ..., T]"]:::sample
    S3["标签y<br/>未来5日收益<br/>(open_T5 - open_T1) / open_T1"]:::sample
    S4["每日排序样本<br/>约300只股票 × 60天 × 特征数"]:::sample
    SO["样本产物<br/>sample_table<br/>sequence_tensor<br/>group_info<br/>rank_label_table"]:::sample
  end

  subgraph P["4. 数据切分 Split"]
    P1["时间切分<br/>不能随机打乱"]:::split
    P2["训练窗口<br/>252个交易日默认"]:::split
    P3["Gap / Purge Zone<br/>5个交易日默认"]:::split
    P4["Walk-forward验证<br/>多轮模拟真实提交"]:::split
    P5["最终全量重训<br/>只使用合法历史样本"]:::split
  end

  subgraph M["5. 模型训练与融合 Model Layer"]
    M1["LightGBM Ranker<br/>主排序模型"]:::model
    M2["收益预测模型<br/>Regressor提供收益大小视角"]:::model
    M3["概率模型<br/>Classifier提供命中率视角"]:::model
    M4["稳健性模型<br/>RandomForest ExtraTrees 可选MLP"]:::model
    M5["融合策略<br/>排名平均 加权融合 OOF Stacking"]:::model
    MO["候选池输出<br/>candidate_top30.csv<br/>stock_id model_score fusion_score rank"]:::model
  end

  subgraph R["6. 精排 Top30 到 result.csv"]
    R1["市场状态Gate<br/>正常 风控 低置信度"]:::rerank
    R2["Hard Gates<br/>流动性 回撤 异常交易 已公开事件"]:::rerank
    R3["Scoring Overlay<br/>0.40 ML_Rank<br/>0.30 Sector_Momentum<br/>0.20 Price_Action<br/>0.10 Style_PE_Rank"]:::rerank
    R4["Portfolio Construction<br/>行业最多2只<br/>相关性阈值<br/>风格暴露控制"]:::rerank
    R5["权重策略<br/>Top5等权<br/>按refine_score加权<br/>风险调整加权<br/>弱势保留现金"]:::rerank
    RO["正式输出<br/>result.csv<br/>最多5只 stock_id weight<br/>ranking_log.csv"]:::rerank
  end

  subgraph E["7. 评分 回测 实验记录 Evaluation Layer"]
    E1["冻结result.csv<br/>冻结后才允许读取test.csv"]:::eval
    E2["官方口径评分<br/>Final Score = Σ return_i × weight_i"]:::eval
    E3["Walk-forward汇总<br/>平均收益 中位数 正收益比例 最大亏损"]:::eval
    E4["实验治理<br/>experiment_id<br/>data feature label split model fusion refine weight版本"]:::eval
    E5["方案保留标准<br/>稳定性 可解释性 防泄漏优先"]:::eval
  end

  Leak["全局防未来信息泄漏检查<br/>预测日T之前不可得的信息不能进入输入 特征 精排 调参"]:::gate

  Start --> D1 --> DQ
  D2 --> DQ
  D3 --> DQ
  D4 --> DQ
  DQ --> F1
  DQ --> F2
  DQ --> F3
  DQ --> F4
  F1 --> FO
  F2 --> FO
  F3 --> FO
  F4 --> FO
  FO --> S1 --> S2 --> S4
  S1 --> S3 --> S4 --> SO
  SO --> P1 --> P2 --> P3 --> P4 --> P5
  P5 --> M1
  P5 --> M2
  P5 --> M3
  P5 --> M4
  M1 --> M5
  M2 --> M5
  M3 --> M5
  M4 --> M5
  M5 --> MO --> R1 --> R2 --> R3 --> R4 --> R5 --> RO
  RO --> E1 --> E2 --> E3 --> E4 --> E5

  Leak -.约束.-> DQ
  Leak -.约束.-> FO
  Leak -.约束.-> SO
  Leak -.约束.-> P3
  Leak -.约束.-> MO
  Leak -.约束.-> RO
  Leak -.约束.-> E1
```

---

## 图 2：数据到样本的可复现链路

```mermaid
flowchart LR
  classDef raw fill:#eef7f1,stroke:#27704f,color:#17201b
  classDef proc fill:#eef4fb,stroke:#286299,color:#17201b
  classDef sample fill:#fff2e8,stroke:#b9612b,color:#17201b
  classDef check fill:#ffffff,stroke:#111827,color:#111827,stroke-width:2px
  classDef out fill:#f1f3f5,stroke:#58616a,color:#17201b

  subgraph Raw["原始数据资产"]
    A1["raw_data<br/>比赛数据和公开行情"]:::raw
    A2["metadata<br/>数据源 下载日期 字段说明 数据版本"]:::raw
    A3["hs300_stock_list<br/>历史截面成分股"]:::raw
    A4["calendar<br/>交易日序列"]:::raw
  end

  subgraph Clean["清洗与对齐"]
    B1["股票代码标准化"]:::proc
    B2["日期按交易日历对齐"]:::proc
    B3["去重 缺失 异常价格 异常成交量处理"]:::proc
    B4["停牌和新股历史不足处理"]:::proc
    B5["股票池截面校验<br/>effective_start_date <= T <= effective_end_date"]:::check
  end

  subgraph Feature["特征表生产"]
    C1["feature_table_daily<br/>个股价量 横截面排名"]:::proc
    C2["market_feature_table<br/>指数趋势 市场波动 涨跌比"]:::proc
    C3["sector_feature_table<br/>行业动能 行业排名"]:::proc
    C4["risk_feature_table<br/>回撤 流动性 相关性基础数据"]:::proc
    C5["fundamental_asof_table<br/>按公告日对齐"]:::proc
    C6["event_asof_table<br/>按公开日对齐"]:::proc
    C7["feature_metadata<br/>来源 窗口 用途 是否调试变量"]:::out
  end

  subgraph Sample["训练样本构造"]
    D1["选择预测日T"]:::sample
    D2["构造输入X<br/>T日及以前60日窗口"]:::sample
    D3["构造标签y<br/>T+1到T+5开盘收益"]:::sample
    D4["每日横截面排序样本<br/>group = date"]:::sample
    D5["样本过滤<br/>窗口不足 标签不完整 关键价格缺失"]:::check
    D6["sample_table"]:::out
    D7["sequence_tensor / window_data"]:::out
    D8["group_info / rank_label_table"]:::out
  end

  A1 --> B1 --> B2 --> B3 --> B4 --> B5
  A2 --> B5
  A3 --> B5
  A4 --> B2
  B5 --> C1
  B5 --> C2
  B5 --> C3
  B5 --> C4
  B5 --> C5
  B5 --> C6
  C1 --> C7
  C2 --> C7
  C3 --> C7
  C4 --> C7
  C5 --> C7
  C6 --> C7
  C7 --> D1
  D1 --> D2
  D1 --> D3
  D2 --> D4
  D3 --> D4
  D4 --> D5
  D5 --> D6
  D5 --> D7
  D5 --> D8

  L1["防泄漏检查<br/>滚动特征只能用T及以前<br/>标准化fit不能提前看全量<br/>标签只能作为答案"]:::check
  L1 -.检查.-> C1
  L1 -.检查.-> C5
  L1 -.检查.-> C6
  L1 -.检查.-> D2
  L1 -.检查.-> D3
```

---

## 图 3：模型召回 Top30 到精排 Top5

```mermaid
flowchart TD
  classDef model fill:#fff7da,stroke:#a47b12,color:#17201b
  classDef pool fill:#eef4fb,stroke:#286299,color:#17201b
  classDef gate fill:#ffffff,stroke:#111827,color:#111827,stroke-width:2px
  classDef score fill:#fff2e8,stroke:#b9612b,color:#17201b
  classDef portfolio fill:#fff0ec,stroke:#b6432e,color:#17201b
  classDef out fill:#f1f3f5,stroke:#58616a,color:#17201b

  Input["第4步合法训练与预测窗口<br/>train validation walk-forward final retrain"]:::out

  subgraph Model["第5步：模型负责召回，不负责最终拍板"]
    M1["LightGBM Ranker<br/>学习每日横截面相对排序"]:::model
    M2["Regressor<br/>估计未来5日收益或超额收益"]:::model
    M3["Classifier<br/>估计正收益或进入Top20概率"]:::model
    M4["RandomForest / ExtraTrees<br/>提供稳健性视角"]:::model
    M5["StockTransformer<br/>作为baseline对照或后期融合"]:::model
    F1["融合<br/>第一版排名平均<br/>第二版加权融合<br/>第三版OOF Stacking"]:::model
    C30["candidate_top30.csv<br/>从300只股票保留前10%左右"]:::pool
  end

  subgraph Rerank["第6步：精排负责最终拍板"]
    G0["市场状态Gate<br/>决定满仓 半仓 低置信度现金"]:::gate
    G1["Hard Gates<br/>流动性不足剔除<br/>大回撤剔除或降权<br/>停牌异常交易剔除<br/>已公开重大风险事件剔除"]:::gate
    S1["ML_Rank<br/>来自融合分数"]:::score
    S2["Sector_Momentum<br/>行业3日或5日动能与资金强度"]:::score
    S3["Price_Action<br/>CLV 下影线 放量收高 尾盘承接"]:::score
    S4["Style_PE_Rank<br/>行业内PE百分位 防御性辅助"]:::score
    RS["refine_score<br/>0.40 ML + 0.30 行业动能 + 0.20 价量行为 + 0.10 风格估值"]:::score
    P1["按refine_score降序遍历"]:::portfolio
    P2["行业分散度<br/>同一申万一级行业最多2只"]:::portfolio
    P3["相关性约束<br/>与已入选股票Corr过高则跳过"]:::portfolio
    P4["风格暴露控制<br/>避免全高Beta或同概念"]:::portfolio
    W1["权重策略<br/>Top5等权0.2<br/>按refine_score加权<br/>按风险调整分数加权"]:::portfolio
    Final["result.csv<br/>最多5只 stock_id weight<br/>weight总和 <= 1"]:::out
    Log["ranking_log.csv<br/>剔除原因 子分数 行业约束 相关性约束 兜底动作"]:::out
  end

  Input --> M1
  Input --> M2
  Input --> M3
  Input --> M4
  Input --> M5
  M1 --> F1
  M2 --> F1
  M3 --> F1
  M4 --> F1
  M5 -.如果walk-forward稳定.-> F1
  F1 --> C30
  C30 --> G0 --> G1
  G1 --> S1
  G1 --> S2
  G1 --> S3
  G1 --> S4
  S1 --> RS
  S2 --> RS
  S3 --> RS
  S4 --> RS
  RS --> P1 --> P2 --> P3 --> P4 --> W1 --> Final
  G1 --> Log
  RS --> Log
  P2 --> Log
  P3 --> Log
  W1 --> Log

  Fallback["兜底规则<br/>Hard Gates后不足5只时<br/>先保留真正硬门槛<br/>再放宽软约束<br/>仍不足则少于5只提交<br/>剩余视为现金"]:::gate
  Fallback -.触发.-> W1
```

---

## 图 4：Walk-forward 实验闭环与治理

```mermaid
flowchart TD
  classDef run fill:#eef4fb,stroke:#286299,color:#17201b
  classDef freeze fill:#fff0ec,stroke:#b6432e,color:#17201b,stroke-width:2px
  classDef eval fill:#f1f3f5,stroke:#58616a,color:#17201b
  classDef decision fill:#ffffff,stroke:#111827,color:#111827,stroke-width:2px
  classDef keep fill:#eef7f1,stroke:#27704f,color:#17201b

  A["定义实验<br/>experiment_id<br/>data feature label split model fusion refine weight版本"]:::run
  B["选择Walk-forward窗口<br/>训练窗口252日默认<br/>Gap 5日默认<br/>评估区间未来5日"]:::run
  C["训练模型并融合<br/>生成candidate_top30.csv"]:::run
  D["精排Top30<br/>生成Top5与权重"]:::run
  E["冻结result.csv<br/>冻结前不能读取test.csv"]:::freeze
  F["官方口径评分<br/>score_self.py<br/>Final Score = Σ return_i × weight_i"]:::eval
  G["记录评分明细<br/>单股收益 权重 贡献<br/>Top30真实召回<br/>行业集中度 市场状态"]:::eval
  H["汇总多轮表现<br/>平均收益 中位数 正收益比例 最大亏损 收益标准差"]:::eval
  I{"是否保留方案？"}:::decision
  J["进入下一轮实验<br/>保留有效变量<br/>调整特征 模型 融合 精排 权重"]:::keep
  K["废弃或降级为对照<br/>单轮幸运 不稳定 最大亏损扩大 召回变差 疑似泄漏"]:::freeze
  L["最终主策略锁定<br/>稳定性优先<br/>可解释性优先<br/>防泄漏优先"]:::keep

  A --> B --> C --> D --> E --> F --> G --> H --> I
  I -- "多轮平均或中位数提升<br/>正收益比例提升<br/>最大亏损可接受<br/>不同市场不过度失效" --> J
  I -- "只靠单轮高分<br/>平均收益无提升<br/>Top30召回变差<br/>疑似未来信息泄漏" --> K
  J --> A
  J --> L
  K --> A

  Leak["评分阶段防泄漏<br/>test.csv不能参与特征工程 标准化fit 模型训练 精排决策<br/>不能用最终test表现反复调权重"]:::decision
  Leak -.约束.-> E
  Leak -.约束.-> F
  Leak -.约束.-> I
```

---

## 图 5：产物与责任边界

```mermaid
flowchart LR
  classDef stage fill:#eef4fb,stroke:#286299,color:#17201b
  classDef file fill:#f8fafc,stroke:#58616a,color:#17201b
  classDef boundary fill:#ffffff,stroke:#111827,color:#111827,stroke-width:2px

  S1["第1步 数据获取"]:::stage --> O1["raw_data<br/>processed_data<br/>metadata"]:::file
  S2["第2步 特征工程"]:::stage --> O2["feature_table_daily<br/>market_feature_table<br/>sector_feature_table<br/>risk_feature_table<br/>fundamental_asof_table<br/>event_asof_table<br/>feature_metadata"]:::file
  S3["第3步 样本构造"]:::stage --> O3["sample_table<br/>sequence_tensor<br/>group_info<br/>rank_label_table<br/>sample_metadata"]:::file
  S4["第4步 时间切分"]:::stage --> O4["split_config<br/>walk_forward_plan<br/>train validation test边界"]:::file
  S5["第5步 模型召回"]:::stage --> O5["candidate_top30.csv<br/>model artifacts<br/>fusion scores"]:::file
  S6["第6步 精排提交"]:::stage --> O6["result.csv<br/>ranking_log.csv"]:::file
  S7["第7步 评分治理"]:::stage --> O7["temp/tmp.csv<br/>evaluation_detail.csv<br/>walk_forward_summary.csv<br/>experiment_log.md或csv"]:::file

  B1["边界1<br/>第5步不能直接输出result.csv"]:::boundary
  B2["边界2<br/>result.csv冻结后才能读取test.csv"]:::boundary
  B3["边界3<br/>评分结果只能用于下一轮实验选择<br/>不能泄漏回本轮提交"]:::boundary

  O5 -.检查.-> B1
  O6 -.检查.-> B2
  O7 -.检查.-> B3
```

---

## 一页执行清单

| 阶段 | 关键输入 | 关键输出 | 必查项 |
|---|---|---|---|
| 1 数据获取 | 行情、股票池、交易日历、指数、行业、基本面、事件 | raw / processed / metadata | 合规、可复现、T日可得、股票池历史截面对齐 |
| 2 特征工程 | 原始数据与辅助表 | feature tables | 滚动窗口只用T及以前、公告日/公开日对齐、标准化不能看全量 |
| 3 样本构造 | feature tables、交易日历 | sample_table、window_data、group_info | X只能用过去，y只能当答案，窗口和标签完整 |
| 4 时间切分 | 样本表 | split_config、walk_forward_plan | 时间不随机打乱、训练与验证间保留Gap、test不调参 |
| 5 模型召回 | 合法训练样本 | candidate_top30.csv | 输出Top30，不直接拍Top5，不只看单轮最高收益 |
| 6 精排提交 | Top30、风险、行业、基本面、事件 | result.csv、ranking_log.csv | Hard Gates、行业相关性约束、weight总和不超过1 |
| 7 评分治理 | 冻结的result.csv、test.csv或未来行情 | Final Score、评分明细、实验日志 | 冻结后评分，评分结果只能用于下一轮复盘 |

---

## 最终心智模型

```text
历史可得数据
  -> 可复现数据资产
  -> 无泄漏特征体系
  -> 每日横截面排序样本
  -> 时间切分与Walk-forward
  -> 多模型融合召回Top30
  -> 金融逻辑精排Top5和权重
  -> 冻结result.csv
  -> 未来5日行情评分
  -> 实验记录和下一轮方案选择
```

一句话版本：

> 我们用历史数据训练排序模型，先从沪深300中召回 Top30 候选池，再通过精排选出最终 Top5 和权重，生成唯一提交的 `result.csv`，由未来真实行情决定成绩。
